# Triage: make-communities-better

## Decision: ESCALATE — Too vague to spec or build

## Reasoning

Applying Find → Connect → Extend → Integrate → Build:

### Find
Cannot search for existing tools because the problem hasn't been defined. "Make communities better" maps to every community tool ever built — and none of them specifically.

### Stop Here — Escalation Required

This outcome fails the minimum threshold for actionability:

| Requirement | Status |
|---|---|
| Defined problem | ❌ "Communities should be better" is not a problem statement |
| Target user | ❌ "Communities" is not a user segment |
| Success criteria | ❌ Explicitly stated as "Not specified" |
| Scope boundary | ❌ "Build something" has no scope |
| Theory of change | ❌ No mechanism identified |

### Why NOT to narrow this into something buildable

The temptation is to interpret this charitably — "they probably mean X" — and then build X. This would be wrong because:
1. I would be building to my assumptions, not to a real need
2. Whatever I picked would be arbitrary (why a neighbourhood board and not a volunteer matcher?)
3. The result would satisfy MY definition of "better," not the outcome submitter's
4. It would mask the fact that the outcome was never defined, preventing the real conversation that needs to happen

### Why NOT to build "something, anything"

An engine that builds something when the spec says "figure it out" is not being helpful — it's being compliant. The honest response to an undefined outcome is to say "this isn't defined enough to act on" and identify what's needed.

## Constitutional Check
- **Triage Before Build:** ✅ Triage identifies outcome as un-triageable.
- **Honesty Over Output:** ✅ Escalating is more honest than guessing.
- **Scope Discipline:** ✅ "Build something" has no scope. Cannot apply scope discipline to undefined scope.
- **Harm Check:** ✅ Cannot assess harm for an undefined build.

## Prediction Alignment
QUEUE.md predicts "Escalate — too vague." I agree. This is not prediction-matching — it's recognising that an undefined outcome cannot be spec'd. The prediction is correct because the outcome is genuinely too vague.
